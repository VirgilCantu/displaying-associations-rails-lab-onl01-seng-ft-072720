<h1>All Artists</h1>


<h3><%= pluralize(@artists.count, "Artist") %></h3>
<uL>
    <% @artists.each do |artist| %>
        <li><%= link_to artist.name, artist_path(artist) %> <%= pluralize(artist.songs.count, "song") %></li>
    <% end %>
</uL>